// enoch-luminaries: a read-only visualization of the Enoch federation.
// It depends on nothing from /enoch at build time — it speaks to the
// federation only over its public HTTP/SSE surface and Bitcoin L1, so
// this module stands alone.
module github.com/enoch/luminaries

go 1.22
